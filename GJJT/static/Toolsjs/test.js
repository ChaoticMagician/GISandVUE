define([

], function() {
    var outtest = "return 外面的值";
   return {
            testvar:'testvar',
            testfunction:function(adfsadf){
                console.log(
                    outtest,adfsadf,
                );
            }
   };
    
});